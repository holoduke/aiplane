import * as THREE from "three";

export const geometry = {
  cube: new THREE.BoxGeometry(200, 200, 200),
  sky: new THREE.SphereGeometry(8000, 64, 64),
  sky2: new THREE.SphereGeometry(5000, 64, 64),
};
